import { ADD_FIGHTER } from '../actions/types'

const initialState = {
    fighters: []
}

export default function(state = initialState, action) {
    switch(action.type) {
        case ADD_FIGHTER:
            return {
          
            }
    }
}